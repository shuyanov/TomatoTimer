import React from "react";
import styles from './Footer.module.scss'
import { FlexContainer } from "../../common";

export const Footer = () => {
    return (
        <FlexContainer width={1920}>
        </FlexContainer>
    )
}
