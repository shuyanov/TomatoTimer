import React from "react";
import styles from './Header.module.scss'
import { FlexContainer } from "../../common";

export const Header = () => {
    return (
        <FlexContainer width={1920} className={styles.Header}>
        </FlexContainer>
    )
}
